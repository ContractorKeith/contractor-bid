---
description: Validate a contractor-bid package before sending it out
argument-hint: "<project-path> --profile <profile-id>"
---

Call `cb_check` for the project and profile. Surface every warning and hard error
before the user prices, submits, or sends the package to suppliers.
